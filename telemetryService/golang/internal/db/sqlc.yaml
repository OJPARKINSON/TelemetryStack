version: "2"
sql:
  - engine: "postgresql"          # QuestDB's PG wire protocol
    queries: "queries/"
    schema: "schema/"
    gen:
      go:
        package: "generated"
        out: "generated"
        sql_package: "pgx/v5"
        emit_json_tags: true